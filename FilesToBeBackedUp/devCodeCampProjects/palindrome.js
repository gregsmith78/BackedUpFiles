"use strict";

function userInput(){
	var userChoice;
	userChoice = window.prompt("Please select a word","");
	return 
}
//returns the first character of the string str
var firstCharacter = function(argument) {
	return str.slice(0 , 1);
};
//returns the last character of the string str
var lastCharacter = function(argument) {
	return str.slice(-1);
};
//returns the string that result from removing the first and last characters from the str  
var middleCharacters = function(argument){
	return str.slice(1,-1);
};
var isPalindrome = function(argument) {
	// base case #1
	// base case #2
	//recursive case
};

var checkPalindrome = function(argument) {
	// body...
} 
    

    if( <= ){
    	return
    }
}
